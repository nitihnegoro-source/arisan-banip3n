rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Paguyuban Bani P3N KUA Kedungbanteng rules
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
